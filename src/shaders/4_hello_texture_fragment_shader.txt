
#version 330 core

in vec3 myColor;
in vec2 myTexCoord;

out vec4 color;

uniform sampler2D myTexture;
void main()
{
    // note that here is component-wise product!
    color = texture(myTexture, myTexCoord) * vec4(myColor, 1.0f);
}